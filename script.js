(function(){const root=document.documentElement;const themeButton=document.querySelector('[data-theme-toggle]');const savedTheme=localStorage.getItem('ficha-theme');if(savedTheme)root.dataset.theme=savedTheme;function updateThemeLabel(){if(themeButton)themeButton.textContent=root.dataset.theme==='dark'?'Modo claro':'Modo oscuro'}updateThemeLabel();themeButton?.addEventListener('click',function(){root.dataset.theme=root.dataset.theme==='dark'?'light':'dark';localStorage.setItem('ficha-theme',root.dataset.theme);updateThemeLabel()});const search=document.querySelector('[data-search]');const filters=Array.from(document.querySelectorAll('[data-filter]'));const notes=Array.from(document.querySelectorAll('[data-note]'));const empty=document.querySelector('[data-empty]');let activeFilter='todos';function applyFilters(){const query=(search?.value||'').trim().toLocaleLowerCase('es');let visible=0;notes.forEach(function(note){const text=note.textContent.toLocaleLowerCase('es');const categories=(note.dataset.category||'').split(' ');const show=(!query||text.includes(query))&&(activeFilter==='todos'||categories.includes(activeFilter));note.hidden=!show;if(show)visible+=1});document.querySelectorAll('.anatomy-area').forEach(function(area){area.hidden=!Array.from(area.querySelectorAll('[data-note]')).some(function(note){return !note.hidden})});empty?.classList.toggle('visible',visible===0)}search?.addEventListener('input',applyFilters);filters.forEach(function(filter){filter.addEventListener('click',function(){activeFilter=filter.dataset.filter;filters.forEach(function(item){item.setAttribute('aria-pressed',String(item===filter))});applyFilters()})})})();
